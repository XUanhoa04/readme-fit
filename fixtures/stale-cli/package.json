{
  "name": "stale-cli",
  "version": "1.0.0",
  "bin": {
    "stale": "dist/cli.js"
  },
  "scripts": {
    "start": "node dist/cli.js",
    "build": "tsc",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  }
}
