# Security policy

Report vulnerabilities privately through GitHub Security Advisories for this repository.

`readme-fit` treats inspected repositories as untrusted. The default scan reads files statically
and does not execute README commands, package scripts, project code, Dockerfiles, or imported
modules. Avoid proposing changes that weaken this boundary.
